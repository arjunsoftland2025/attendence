from setuptools import setup, find_packages

setup(
    name='attendence',
    version='0.0.1',
    description='attendence app',
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    zip_safe=False
)
